import { Link } from "react-router-dom";

const NotFound = () => {
  return (
    <main>
      <h1>NOT FOUND!</h1>
      <Link to="/">Back to Homepage</Link>
    </main>
  );
};

export default NotFound;
